using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ReflectGraph : MonoBehaviour
{ // reflect here means flip
    // to reflect F(x) about x axis : g(x) = -f(x)
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
